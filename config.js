const CONFIG = {
    // Substitua pelo ID real do projeto Firebase do seu catálogo online
    FIREBASE_PROJECT_ID: "seu-projeto-id-aqui", 
    // Nome da tabela onde estão os clientes
    COLLECTION_NAME: "clientes",
    // Configurações de segurança Anti-Ban (em milissegundos)
    DELAY_MIN: 3000,
    DELAY_MAX: 7000
};

export default CONFIG;
